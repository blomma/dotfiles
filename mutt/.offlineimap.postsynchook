#!/bin/sh
notmuch new

notmuch tag -inbox +spam -- tag:inbox and subject:"SPAM"

notmuch tag -inbox +outback -- tag:inbox and to:"theoutback@samkieth.com"
notmuch tag -inbox +outback -- tag:inbox and from:"theoutback@samkieth.com"
notmuch tag -inbox +outback -- tag:inbox and to:"*outback*"
